import 'package:flutter/material.dart';

class AdminCropScreen extends StatelessWidget {
  const AdminCropScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text("Admin Crop Screen"));
  }
}
