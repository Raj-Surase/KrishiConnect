import 'package:flutter/material.dart';

class UserSchemesScreen extends StatelessWidget {
  const UserSchemesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text("Schemes"));
  }
}
