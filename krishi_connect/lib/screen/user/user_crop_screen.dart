import 'package:flutter/material.dart';

class UserCropScreen extends StatelessWidget {
  const UserCropScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text("Crop"));
  }
}
